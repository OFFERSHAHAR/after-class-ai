{
  "name": "After Class AI - Lead Bot Template",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "after-class-lead",
        "responseMode": "lastNode",
        "options": {}
      },
      "id": "webhook-lead-input",
      "name": "Lead Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [240, 300],
      "webhookId": "after-class-lead"
    },
    {
      "parameters": {
        "jsCode": "const lead = $json.body || $json;\nconst score = lead.message && lead.message.length > 80 ? 'high' : 'medium';\nreturn [{ json: { ...lead, ai_priority: score, classroom_note: 'Demo classifier placeholder' } }];"
      },
      "id": "demo-ai-classifier",
      "name": "Demo AI Classifier",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [520, 300]
    }
  ],
  "connections": {
    "Lead Webhook": {
      "main": [
        [
          {
            "node": "Demo AI Classifier",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  }
}
