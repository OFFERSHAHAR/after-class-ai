# כלים

שים כאן:

```text
ngrok.exe
```

המיקום צריך להיות:

```text
classroom-run/tools/ngrok.exe
```

אם הקובץ לא נמצא כאן, ערכת ההרצה עדיין תפעיל את הממשק ואת סביבת העבודה, אבל לא תפתח קישורים ציבוריים.

